import { describe, expect, it } from "vitest";

describe("CV-Builder Page", () => {
	it("renders all CV-Builder page sections", () => {
		expect(true).toBe(true);
	});

	it("renders the CV-Builder page", () => {
		expect(true).toBe(true);
	});
});
